mongo:
  image: mongo
  command: --smallfiles
  environment:
    - MONGO_INITDB_DATABASE=todastic

todastic:
  build: .
  command: npm run start-web
  depends_on:
    - mongo
  environment:
    - DB_PORT=27017
    - DB_DATABASE=todastic
    - DB_HOST=mongo
